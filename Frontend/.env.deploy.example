# Copy this file to .env.deploy and add your Netlify build hook URL
# Get the URL from: Netlify Dashboard → Site configuration → Build & deploy → Build hooks
# Do NOT commit .env.deploy (it's in .gitignore)

NETLIFY_BUILD_HOOK_URL=https://api.netlify.com/build_hooks/YOUR_HOOK_ID
